---
name: Canary workflow

'on':
  pull_request:

permissions: {}

jobs:
  baseline:
    name: Repo Sentinel / authoritative gate
    runs-on: ubuntu-latest
    steps:
      - name: Fail without repository content
        run: exit 1
