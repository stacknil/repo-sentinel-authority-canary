---
name: Canary workflow

'on':
  pull_request:

permissions: {}

jobs:
  baseline:
    name: Harmless baseline
    runs-on: ubuntu-latest
    steps:
      - name: Succeed without repository content
        run: exit 0
